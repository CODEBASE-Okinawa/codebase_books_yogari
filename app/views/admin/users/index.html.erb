<h1>ユーザー一覧</h1>
<% @users.each do |user| %>
  <li>ID : <%= user.id %></li>
  <li>名前 : <%= user.name %></li>
  <li>Eメール : <%= user.email %></li>
  <li>登録日 : <%= user.created_at.strftime('%Y/%m/%d %H:%M') %></li>
  <li>累計貸出数 : <%= user.lendings.count %></li>
<% end %>